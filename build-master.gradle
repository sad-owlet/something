ext {
    snapshot = false

    jdbcUrl = "jdbc:postgresql://db.ext-system.com/firmware_manager"
    jdbcUserName = "firmware_manager"
    jdbcPassword = "firmware_manager"
    fwCleanDisabled = false
}
